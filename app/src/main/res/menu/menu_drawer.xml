<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:id="@+id/group_lamp" android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_map"
            android:checked="true"
            android:icon="@drawable/ic_map_24dp"
            android:title="Map" />
        <item
            android:id="@+id/drawer_history"
            android:checked="true"
            android:icon="@drawable/ic_history_24dp"
            android:title="History" />
    </group>

    <group android:id="@+id/group_teine" android:checkableBehavior="single">
            <item
                android:id="@+id/drawer_settings"
                android:icon="@drawable/ic_settings_24dp"
                android:title="@string/settings" />
            <item
                android:id="@+id/drawer_feedback"
                android:icon="@drawable/ic_help_24dp"
                android:title="@string/help" />
    </group>
</menu>